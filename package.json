{
  "name": "pathflow-acquisitions-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "verify:deployment-target": "node scripts/verify-acquisitions-target.mjs",
    "build": "npm run verify:deployment-target && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name pathflow-acquisitions-website --branch main",
    "router:deploy": "wrangler deploy --config workers/acquisitions-router/wrangler.toml",
    "router:typecheck": "tsc -p workers/acquisitions-router/tsconfig.json",
    "worker:login": "wrangler login",
    "worker:whoami": "wrangler whoami",
    "worker:dev": "wrangler dev --config workers/contact/wrangler.toml",
    "worker:deploy": "wrangler deploy --config workers/contact/wrangler.toml",
    "worker:secret:resend": "wrangler secret put RESEND_API_KEY --config workers/contact/wrangler.toml",
    "worker:secret:turnstile": "wrangler secret put TURNSTILE_SECRET_KEY --config workers/contact/wrangler.toml",
    "worker:tail": "wrangler tail --config workers/contact/wrangler.toml",
    "worker:typecheck": "tsc -p workers/contact/tsconfig.json"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "@playwright/test": "^1.63.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "wrangler": "^4.138.0"
  }
}
